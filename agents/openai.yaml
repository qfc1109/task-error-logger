interface:
  display_name: "Task Plan Maintainer"
  short_description: "维护清晰的 task_plan.md"
  default_prompt: "使用 $maintaining-task-plan 更新 task_plan.md：只把未结束、后面还要继续跟的需求放进需求索引；已完成需求只保留在日期记录或历史日期记录中；每条日期记录必须能对应到需求ID和阶段。"

policy:
  allow_implicit_invocation: true
